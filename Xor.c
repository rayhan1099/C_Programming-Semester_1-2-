#include <stdio.h>
#include <stdlib.h>

int main()
{int a,b,c;
scanf("%d%d",&a,&b);
if(a==0||a==1&&b==0||b==1)
{
    if(a==b)
    {
        printf("0");}
        else
        {printf("1");
    }
}
    return 0;
}
