#include <stdio.h>
#include <math.h>


int main ()
{
	int i,n,r,k,p,sum,q;
	scanf("%d",&n);
	if(n==2)
	  {for (i=1;i<n;i++)
	{
		scanf("%d%d",&r,&p);
        k=r+p;
       }{
       printf("%d\n",k);}}
       else
         {{k=0;{
            for(i=1;i<n;i++)
       {

            scanf("%d%d",&r,&p);
             k=k+p+r;
           }sum=k+0;}}
        q=sum+i;
        printf("%d",q);}

	return 0;
}
